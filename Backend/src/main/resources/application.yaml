logging:
  level:
    org.springframework: DEBUG


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: "translateryildiz@gmail.com"
    password: "ryou xved iwwh pcjd"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true